import{$,notify}from'../utils/helpers.js';import{login,register,users}from'../services/auth-service.js';
export const initLogin=()=>{$('#login-form').addEventListener('submit',async e=>{e.preventDefault();try{await login($('#email').value,$('#password').value);notify('¡Bienvenido/a de nuevo!');location.href='index.html'}catch(err){window.Swal.fire('No fue posible iniciar sesión',err.message,'error')}});$('#forgot-password').onclick=()=>window.Swal.fire({title:'Recuperación simulada',input:'email',inputLabel:'Escribe tu correo',showCancelButton:true,confirmButtonText:'Enviar'}).then(r=>{if(r.isConfirmed)window.Swal.fire(users().some(u=>u.email===r.value.trim().toLowerCase())?'Solicitud registrada':'Correo no encontrado','En un entorno real recibirías instrucciones en tu correo.','info')})};
export const initRegister=()=>{$('#register-form').addEventListener('submit',async e=>{e.preventDefault();const name=$('#name').value,email=$('#email').value,password=$('#password').value,confirm=$('#password-confirm').value;if(password.length<8) return window.Swal.fire('Contraseña débil','Usa al menos 8 caracteres.','warning');if(password!==confirm)return window.Swal.fire('Las contraseñas no coinciden','','warning');try{await register({name,email,password,community:$('#community').value});notify('Cuenta creada correctamente.');location.href='index.html'}catch(err){window.Swal.fire('No fue posible crear la cuenta',err.message,'error')}})};
